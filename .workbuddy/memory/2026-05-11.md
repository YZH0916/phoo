# 2026-05-11 工作记录

## 部署 ASD-image-classifier-Python
- 从 GitHub 下载仓库：https://github.com/RxinnotRstar/ASD-image-classifier-Python
- 解压至：`d:\workbuddy\WB project\WB009asd-\ASD-image-classifier-Python-main\`
- 安装依赖：pillow 12.1.0、pywin32（已安装到系统 Python 3.11）
- 程序已成功启动，入口：`image_classifier.py`
- 快捷键：A/S/D 分类到三个文件夹，W/X 切换文件，Ctrl+Z 撤销

## 优化 image_classifier.py（三项功能增强）
- **多文件夹支持**：从固定3个扩展到最多9个，Spinbox（2~9）动态控制数量，输出行动态重建
- **自定义快捷键**：新增"⌨ 自定义快捷键"按钮，弹出对话框逐一设置，默认 ASDFGHJKL，冲突/保留键检测
- **Tab 切换模式**：Tab 键一键切换复制/移动模式，状态栏实时显示当前模式并闪现临时提示
- 配置持久化：folder_count 和 hotkeys 已加入 classifier_config.json

## 界面美化
- 全局字体从 size=20 降至 size=11，移除强制 Microsoft YaHei 改用系统默认
- 统一配色方案：蓝色主按钮、灰色辅助按钮、浅灰背景 `#f0f2f5`
- 所有按钮增加 padx/pady/bd=0/cursor='hand2' 样式
- 图片预览区边框改为 highlightborder 细线
- 状态栏改为浅灰底小字号，显示当前模式
- 欢迎页精简文字、左对齐
- 快捷键对话框、视频提示文字等全部统一字号

## 快捷创建分类文件夹
- 每个输出文件夹输入框右侧新增绿色「✚」按钮
- 点击后在输入文件夹同级目录下自动创建 `分类1`、`分类2`... 子文件夹
- 自动填入路径并保存配置，支持重名自动追加序号（`分类1_2`）
- 修复 IME 冲突：弃用 StringVar trace，改用 Entry.insert/get，grab_set 延迟100ms
- 修复 ✚ 按钮被挤出：将 pack(side=LEFT) 改为 grid(3列) 布局

## 进度显示与断点续传（2026-05-12）
- 状态栏增强：显示"待分类/已分类/当前文件/模式"
- 状态栏右侧增加 ttk.Progressbar（百分比进度条）+百分比标签
- 每个输出文件夹按钮右侧新增计数标签（📁N张 / 📂空）
- 断点续传：save_config 保存 last_ptr + last_anchor（当前文件名），load_config 恢复，load_images 按锚文件名精确定位 ptr
- WM_DELETE_WINDOW 绑定 _on_close 代替 atexit，确保关闭时必定保存进度
